# Experiment 2 — React State Management & Custom Hooks

## Planned Topics
- Component State with `useState` and `useEffect`
- Custom Hooks (`useTasks`, `useLocalStorage`, `useModal`, `useDebounce`)
- Task creation, editing, and column state updates in memory / local storage
