# Experiment 9 — Containerization with Docker & Docker Compose

## Planned Topics
- Frontend `Dockerfile` with multi-stage Nginx build
- Backend `Dockerfile` with Node.js Alpine image
- `docker-compose.yml` orchestrating MongoDB, Backend, and Frontend containers
