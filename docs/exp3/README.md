# Experiment 3 — Global State with Context API & Reducers

## Planned Topics
- `BoardContext` with `useReducer` for task actions (`ADD_TASK`, `MOVE_TASK`, `DELETE_TASK`)
- `AuthContext` for user session management
- Filter & Search state synchronization across views
